---
description: Run the right checks for the current work
argument-hint: "[scope]"
---
Verify ${ARGUMENTS:-the current working tree changes}. Discover the repository's documented test, lint, type-check, and build commands. Run focused checks first and broader checks when justified by the blast radius. Fix failures caused by the current changes; distinguish unrelated pre-existing failures clearly. Finish with the exact checks and outcomes.
