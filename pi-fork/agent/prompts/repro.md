---
description: Build a minimal reproduction of a bug
argument-hint: "<bug description>"
---
Build a minimal reproduction for: $ARGUMENTS

Inspect the relevant code first, then create the smallest standalone script or test that triggers the bug (prefer the repo's existing test framework). Include the expected vs actual behavior, and the exact command to run it. Do not fix the bug itself unless asked.
