---
description: Design and run focused tests for a change
argument-hint: "[scope]"
---
Design focused tests for ${ARGUMENTS:-the current changes}, then inspect the existing test conventions and implement only the tests needed to cover the important behavior. Run the narrowest relevant test command and report failures precisely. Do not broaden scope into unrelated refactoring.
